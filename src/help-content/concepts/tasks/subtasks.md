# Sub-tasks

A sub-task is a Task like any other, linked to a parent Task instead of a
Project. The parent shows a progress bar rolled up from its sub-tasks, but
nothing auto-completes or cascades — parent and child status stay manual
unless you change them yourself.
